{
  "build": {
    "buildCommand": "cd backend && pip install -r requirements.txt",
    "runCommand": "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
  },
  "deploy": {
    "numReplicas": 1
  }
}